Public Class frmShowCarImages

    Private Sub frmShowCarImages_Load(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles MyBase.Load
        '    Dim arraysize As Integer
        '    FileOpen(1, "carfile.txt", OpenMode.Binary)
        '    arraysize = LOF(1) / Len(ThisCar)
        '    Dim pic(arraysize) As PictureBox
        '    Dim row, col As Integer
        '    row = 1
        '    col = 1

        '    Do While Not EOF(1)
        '        FileGet(1, ThisCar)
        '        FileName = ThisCar.imagefile
        '        pic(x) = New PictureBox()
        '        pic(x).image = Image.FromFile(FileName)
        '        pic(x).sizemode = PictureBoxSizeMode.StretchImage


        '    Loop
    End Sub
End Class